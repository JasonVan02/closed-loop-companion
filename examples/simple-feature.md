# CSV export; a typo

**User:** “Use $closed-loop-companion to add transaction CSV export. Export the current filtered rows, preserve sorting, use UTF-8 and add no server persistence.”

**Route:** FULL — observable new behavior with data-selection and output-format criteria.

**Flow:** inspect project → contract → implement → test → evidence → independent internal review → real ChatGPT review → repair/recheck if needed → gate.

**Outcome:** READY_FOR_USER_ACCEPTANCE only if the declared FULL conditions pass. Missing real ChatGPT capability → LOCAL_REVIEW_ONLY with the FULL gate unmet.

**Small request:** “Use $closed-loop-companion to fix this typo.” → DIRECT → inspect the actual change → report it for acceptance. If the user adds “I want full independent review anyway,” use FULL and check capabilities.
